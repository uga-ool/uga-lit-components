{
  "name": "uga-lit-vite",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:zip": "npm run build && npm run zip",
    "zip": "node scripts/zip-dist.mjs"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "glob": "^10",
    "rollup": "^4",
    "vite": "^5"
  }
}
