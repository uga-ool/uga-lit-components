# Optional: External API endpoint for submitting quiz grades to eLC.
# If set, the quiz will POST results here; the API service then writes to D2L.
# See uga-lit-components/api-service/README.md for setup.
# VITE_QUIZ_API_ENDPOINT=https://your-api-domain.com/api/quiz/submit
