---
description: Build uga-components.js bundle and verify dist output
---

You are helping build the **uga-lit-components** production bundle.

1. From the repo root, run `npm run build`.
2. Confirm `dist/js/uga-components.js` exists and report file size.
3. If build fails, fix only issues related to the user's task — do not refactor unrelated components.
4. Remind: eLC deploy path is typically `/shared/ugaonline/js/uga-components.js` (see README).

Do not commit unless the user asks.
