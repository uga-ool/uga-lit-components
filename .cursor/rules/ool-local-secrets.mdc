---
description: Never commit secrets; no student PII in AI prompts or repo files
alwaysApply: true
---

# Local secrets and FERPA (OOL)

## Secrets stay on your computer

- **Never** commit `.env`, `.env.local`, API keys, OAuth client secrets, Kaltura admin secrets, or LLM provider keys.
- Keep real credentials in **gitignored** files on your machine only. Use `.env.example` for placeholder names, not real values.
- In PR descriptions and screenshots, **redact** tokens and credentials.

## FERPA

- Do not paste **student names**, **UGA IDs**, or **identifiable grade data** into AI prompts or committed files. 
- When in doubt, remove identifying fields.

## More help

- [`docs/cursor/secrets.md`](../docs/cursor/secrets.md)
